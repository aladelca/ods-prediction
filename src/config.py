"""Project-level paths and default runtime settings."""

from __future__ import annotations

from pathlib import Path

ROOT_DIR = Path(__file__).resolve().parents[1]

DATA_PATH = ROOT_DIR / "data" / "train.xlsx"
ARTIFACTS_DIR = ROOT_DIR / "artifacts"

# Notebook winner artifacts
BEST_MODEL_PATH = ARTIFACTS_DIR / "best_model.joblib"
BEST_METADATA_PATH = ARTIFACTS_DIR / "best_model_metadata.json"

# Deployment artifacts (generated by src.train_pipeline)
DEPLOY_MODEL_PATH = ARTIFACTS_DIR / "deploy_model.joblib"
DEPLOY_METADATA_PATH = ARTIFACTS_DIR / "deploy_model_metadata.json"
DEPLOY_METRICS_PATH = ARTIFACTS_DIR / "deploy_metrics.json"

DEFAULT_RANDOM_STATE = 42
DEFAULT_TRAIN_SIZE = 0.70
DEFAULT_VAL_SIZE = 0.15
DEFAULT_TEST_SIZE = 0.15
